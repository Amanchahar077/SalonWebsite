server:
  forward-headers-strategy: framework
  servlet:
    session:
      cookie:
        same-site: none
        secure: true

spring:
  jpa:
    hibernate:
      ddl-auto: validate

management:
  endpoint:
    health:
      probes:
        enabled: true
